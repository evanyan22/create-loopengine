ANTHROPIC_API_KEY=
# Using OpenAI instead? Swap agents/example-agent.ts's createModelCall for
# createOpenAIModelCall({ model: '...' }) and set this instead:
# OPENAI_API_KEY=

PORT=8787

# Optional — omit for local, file-based sessions under .sessions/. Set this
# once you run more than one server instance against the same sessions.
# REDIS_URL=redis://localhost:6379
